# Backend TODO

- Select authenticated API/runtime and PostgreSQL host.
- Add BusinessMembership roles and server-enforced business scope.
- Convert mock workspace actions into transactions.
- Add immutable document snapshot pipeline and secure object storage.
- Add signed, expiring, revocable customer links.
- Add file validation, virus scanning, quotas, and signed access URLs.
- Add Helper Access grants, invitations, audit, expiry, and revocation.
- Add admin-managed service catalog and kit versioning.
- Add integration secrets only when each provider implementation begins.
